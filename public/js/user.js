class User {
    constructor() {
        this.username = username;
        this.password = password; //I will obviously encrypt the password at some point prior to production, this is just basic planning, its a large project
    }
} 

class User_Settings {
    // Contains any attributes a user can change (option to allow overlapping tasks n stuff)
}